<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cursoandroid.organizzeclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Activity.ReceitasActivity"
            android:theme="@style/receitaTheme"></activity>
        <activity android:name=".ui.Activity.DespesasActivity"
            android:theme="@style/despesaTheme"/>
        <activity android:name=".ui.Activity.SemiLoginActivity" />
        <activity
            android:name=".ui.Activity.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.Activity.CadastroActivity" />
        <activity android:name=".ui.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>